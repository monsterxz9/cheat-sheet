### 一、章节标题（蓝色色块，跨全栏）
#### （一）小节标题（蓝色左边框）
1. **关键词红色高亮** — 后面跟简要说明
2. **第二个要点** — 用破折号连接解释
   - 子要点用无序列表
   - 尽量一行说清楚

#### （二）使用技巧
- 用 `###` 做大章标题，`####` 做小节标题
- 用 `**加粗**` 标记关键词，会自动变红
- 用 `—` 破折号连接关键词和解释，比冒号更省空间
- 列表比段落更紧凑，优先用列表
- 避免长段落，一句话能说清的不要写两句

### 二、排版参数参考

#### （一）默认参数（适合大多数场景）
- 字号 8px，行距 1.1，双栏
- A4 纸，页边距 1-2mm
- 字体 Songti SC（macOS）

#### （二）内容太多放不下？
- 字号降到 7px（中文最小可读极限）
- 行距降到 1.05
- 改三栏 `columns: 3`
- 精简内容：删例子、用箭头 → 代替"导致"

#### （三）内容太少有空白？
- 字号升到 9-10px
- 行距升到 1.2
- 保持双栏
